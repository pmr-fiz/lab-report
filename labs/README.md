Папки лаб создаются автоматически. Просто пришли агенту файл задания
(`.docx`/`.pdf`) в чат — он сам создаст `labs/<N>/` и положит туда задание,
код (в структуре из методички или по файлу `src/taskK.py` на задание), отчёт и
разбор.
